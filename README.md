# Password generator
with following features:
- **Customizable Password Length**
- **Optional Character Types**:  
  - Special characters (e.g., `@`, `#`, `!`)  
  - Uppercase letters (e.g., `A`, `B`, `C`)  
  - Lowercase letters (e.g., `a`, `b`, `c`)  
  - Digits (e.g., `0`, `1`, `2`)  
- **Secure Generation**: Random passwords generated by using SecureRandom library.  

**Installation**
--------------------------
`git clone https://github.com/serx27/password-gen.git`
